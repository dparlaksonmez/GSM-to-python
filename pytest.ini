[pytest]
testpaths = tests
python_files = test_*.py
python_functions = test_*
addopts = -v --tb=short -W default::UserWarning
markers =
    slow: marks tests as slow / optional (run with: pytest -m slow)
