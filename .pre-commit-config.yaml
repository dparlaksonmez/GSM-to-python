repos:
  - repo: local
    hooks:
      - id: pytest
        name: Run unit tests
        entry: pytest tests/ --tb=short -q
        language: system
        pass_filenames: false
        always_run: true
        stages: [pre-push]
